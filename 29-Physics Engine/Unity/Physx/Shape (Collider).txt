we can attach shape to actor (attachShape method on actor) as collider with specified properties
properties like geometry,material
we can set flags like trigger on shape as well to make it trigger collider
