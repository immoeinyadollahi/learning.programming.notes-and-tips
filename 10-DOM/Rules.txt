1. if something’s in HTML, then it also must be in the DOM tree.Everything in HTML, even comments, becomes a part of the DOM,Even the <!DOCTYPE...>

2. dom properties that is setter,only do small things and never do works like dispatching events but will update ui